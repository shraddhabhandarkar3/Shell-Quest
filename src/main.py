"""ShellQuest entry point.

Wires together the engine (sandbox, runner, checker, theme_builder) and the
integrations (Box, Apify) into the interactive game loop. Implemented fully in
TICKET-7; for now this is an empty entry point so the package is runnable.
"""


def main() -> None:
    """Launch the ShellQuest game loop. Implemented in TICKET-7."""
    raise NotImplementedError


if __name__ == "__main__":
    main()
